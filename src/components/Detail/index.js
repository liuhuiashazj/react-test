import React from 'react'
import $ from 'jquery'
export const Detail = (props) => {
	console.log($);
    return (
        < div >
        this is Detail { props.data } < /div>
    )
}

export default Detail
